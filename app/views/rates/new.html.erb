<h1>New transfer rate</h1>

<%= @transferrate %>
<%= form_for :rate, url: rates_path do |tr| %>

  <% if @rate.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rate.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @rate.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'form' %>
  
<% end %>

<%= link_to 'Back', rates_path %>